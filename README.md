# laravel-chatgpt-example
Instructions on how to install your project. Include any dependencies that need to be installed.
- composer install
- npm install
- add OPENAI_API_KEY to the .env file
- run php artisan migrate
